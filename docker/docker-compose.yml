services:
  internship-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "${HOST_PORT:-8080}:8080"
    volumes:
      - ../data:/app/data:ro
      - ../userdata:/app/userdata
    environment:
      - PORT=8080
    restart: unless-stopped
